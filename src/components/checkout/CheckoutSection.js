import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { theme } from '../../theme';

/**
 * CheckoutSection - Card-like container for checkout sections
 * Matches EazMain web design with clean spacing and shadows
